<!--


  *This file is automatically generated. Local changes risk being overwritten by the export process.*


-->

<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

  <PropertyGroup>

    <UseDefaultPropertyPageSchemas>false</UseDefaultPropertyPageSchemas>

    <UseDefaultGeneralPropertyPageSchema>false</UseDefaultGeneralPropertyPageSchema>

    <UseDefaultDebuggersPropertyPageSchemas>false</UseDefaultDebuggersPropertyPageSchemas>

    <UseDefaultProjectTools>false</UseDefaultProjectTools>

  </PropertyGroup>

  <PropertyGroup>

    <PlatformPrepareForBuildDependsOn>PlatformToolsetPrepareForBuild;$(PlatformPrepareForBuildDependsOn)</PlatformPrepareForBuildDependsOn>

    <PlatformToolsetPrepareForBuildDependsOn></PlatformToolsetPrepareForBuildDependsOn>

    <TrackFileAccess Condition="'$(TrackFileAccess)' == ''">true</TrackFileAccess>

  </PropertyGroup>

  <Target Name="PlatformToolsetPrepareForBuild" DependsOnTargets="$(PlatformToolsetPrepareForBuildDependsOn)">

    <!-- JDK -->
    <Error Condition="'$(JavaHomeDir)' == ''" Text="Could not locate the Java JDK." />
    <Error Condition="!Exists('$(JavaHomeDir)')" Text="Could not locate the Java JDK. Path does not exist. Expected: $(JavaHomeDir)." />

    <!-- Validate existence of Android SDK -->
    <Error Condition="'$(AndroidSdkRoot)' == ''" Text="Could not locate the Android SDK." />
    <Error Condition="'$(AndroidSdkRoot)' == 'AndroidSdkRoot_is_not_defined'" Text="Could not locate the Android SDK." />
    <Error Condition="!Exists('$(AndroidSdkRoot)')" Text="Could not locate the Android SDK. Path does not exist. Expected: $(AndroidSdkRoot)." />

    <!-- Validate existence of Android NDK -->
    <Error Condition="'$(AndroidNdkRoot)' == ''" Text="Could not locate the Android NDK." />
    <Error Condition="'$(AndroidNdkRoot)' == 'AndroidNdkRoot_is_not_defined'" Text="Could not locate the Android NDK." />
    <Error Condition="!Exists('$(AndroidNdkRoot)')" Text="Could not locate the Android NDK. Path does not exist. Expected: $(AndroidNdkRoot)." />

    <!-- SDK/NDK levels -->
    <Error Condition="'$(AndroidSdkLevel)' == ''" Text="Could not determine Android SDK target API." />
    <Error Condition="'$(AndroidNdkLevel)' == ''" Text="Could not determine Android NDK target API." />

  </Target>

  <Import Project="$(VCTargetsPath)\Microsoft.CppCommon.targets" />

  <Import Project="$(MSBuildThisFileDirectory)\Android.targets" />

  <Import Project="$(MSBuildThisFileDirectory)\Cpp.targets" />

  <Import Project="$(MSBuildThisFileDirectory)\ProjectTypes\DeployViaMsBuild.targets" />

</Project>
